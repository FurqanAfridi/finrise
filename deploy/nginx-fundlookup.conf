# Same Next.js process on port 3001. Point apex, www, app, and admin here.
# After adding app.fundlookup.co: certbot --nginx -d fundlookup.co -d www.fundlookup.co -d app.fundlookup.co -d admin.fundlookup.co

# Example (adjust certificate paths after certbot):
#
# server {
#   listen 443 ssl http2;
#   server_name fundlookup.co www.fundlookup.co app.fundlookup.co admin.fundlookup.co;
#
#   location / {
#     proxy_pass http://127.0.0.1:3001;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#   }
# }
